# 51 单片机中断系统 — 理论预习笔记

> 预习时间：2026-07-10 | 教材：江协科技 51 教程 + STC89C52 数据手册
>
> 下一步：外部中断 INT0/INT1 实验 → PWM

---

## 一、什么是中断

CPU 正在执行主程序时，外部或内部发生了紧急事件 → CPU 暂停当前任务 → 转去处理中断服务函数 → 处理完后返回继续执行主程序。

**类比**：你在写作业（主程序），电话响了（中断请求）→ 接电话（中断响应）→ 挂了继续写作业（中断返回）。

---

## 二、51 单片机中断系统结构

```
                    ┌─────────────────────────┐
                    │         CPU              │
                    │      (响应中断)           │
                    └──────────┬──────────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
         ┌────┴────┐    ┌─────┴─────┐    ┌─────┴─────┐
         │  INT0   │    │  Timer0   │    │   INT1    │
         │ P3.2    │    │  内部溢出  │    │  P3.3     │
         └─────────┘    └───────────┘    └───────────┘
              │                │                │
         ┌────┴────┐    ┌─────┴─────┐    ┌─────┴─────┐
         │ Timer1  │    │  串口中断  │    │           │
         │ 内部溢出 │    │  RI/TI    │    │           │
         └─────────┘    └───────────┘    └───────────┘
```

**5 个中断源**：

| 中断源 | 入口地址 | interrupt 编号 | 触发引脚 | 触发条件 |
|--------|----------|---------------|----------|----------|
| INT0（外部中断0）| 0x0003 | 0 | P3.2 | 低电平/下降沿 |
| Timer0（定时器0）| 0x000B | 1 | 内部 | TF0=1（溢出） |
| INT1（外部中断1）| 0x0013 | 2 | P3.3 | 低电平/下降沿 |
| Timer1（定时器1）| 0x001B | 3 | 内部 | TF1=1（溢出） |
| 串口 | 0x0023 | 4 | 内部 | RI=1 或 TI=1 |

> 🔑 入口地址之间的间距只有 8 字节，通常放一条 `LJMP` 指令跳转到真正的中断服务函数。

---

## 三、核心寄存器

### 3.1 中断使能寄存器 IE（0xA8）

| 位 | 7 | 6 | 5 | 4 | 3 | 2 | 1 | 0 |
|----|---|---|---|---|---|---|---|---|
| 符号 | EA | — | — | ES | ET1 | EX1 | ET0 | EX0 |
| 说明 | 总开关 | — | — | 串口 | 定时器1 | 外部1 | 定时器0 | 外部0 |

- **EA = 1**：打开总中断（任何中断要使能，EA 必须为 1）
- **EX0 = 1**：使能外部中断 0
- **ET0 = 1**：使能定时器 0 中断
- **EX1 = 1**：使能外部中断 1
- **ET1 = 1**：使能定时器 1 中断
- **ES = 1**：使能串口中断

### 3.2 定时器控制寄存器 TCON（0x88）— 中断相关位

| 位 | 7 | 6 | 5 | 4 | 3 | 2 | 1 | 0 |
|----|---|---|---|---|---|---|---|---|
| 符号 | TF1 | TR1 | TF0 | TR0 | IE1 | IT1 | IE0 | IT0 |

**外部中断相关**：
- **IT0**：INT0 触发方式选择（0=低电平触发，1=下降沿触发）
- **IE0**：INT0 中断请求标志（硬件自动置 1→响应后自动清 0）
- **IT1**：INT1 触发方式选择
- **IE1**：INT1 中断请求标志

> ⚠️ **低电平触发 vs 下降沿触发**：
> - 低电平触发（IT0=0）：引脚低电平时持续触发 → 可能重复进入中断
> - 下降沿触发（IT0=1）：只在高→低跳变瞬间触发一次 → 更常用

### 3.3 中断优先级寄存器 IP（0xB8）

| 位 | 7 | 6 | 5 | 4 | 3 | 2 | 1 | 0 |
|----|---|---|---|---|---|---|---|---|
| 符号 | — | — | — | PS | PT1 | PX1 | PT0 | PX0 |

- **PX0 = 1**：INT0 高优先级
- **PT0 = 1**：Timer0 高优先级
- **PX1 = 1**：INT1 高优先级
- **PT1 = 1**：Timer1 高优先级
- **PS = 1**：串口高优先级

> 🔑 **自然优先级**（同优先级时硬件排队）：INT0 > Timer0 > INT1 > Timer1 > 串口

---

## 四、中断响应过程

```
1. 中断源发出请求（硬件置位 IE0/TF0/IE1/TF1/RI/TI）
        ↓
2. CPU 查询（每个机器周期）— 需满足条件：
   - EA = 1（总中断开）
   - 对应中断允许位 = 1
   - 无同级或更高级中断正在处理
   - 当前指令执行完毕
        ↓
3. 硬件自动：
   - PC 入栈保护
   - 跳转到中断入口地址
        ↓
4. 执行中断服务函数（ISR）
        ↓
5. RETI 指令：
   - PC 出栈恢复
   - 清除中断标志（部分自动）
   - 返回断点继续执行
```

> 🔑 中断标志清除规则：
> - INT0/INT1（下降沿触发）：硬件自动清除 IE0/IE1
> - Timer0/Timer1：硬件自动清除 TF0/TF1
> - 串口：**必须软件清除** RI/TI（`RI = 0; TI = 0;`）
> - INT0/INT1（低电平触发）：必须外部拉高引脚才能清除！

---

## 五、代码模板

### 5.1 外部中断 INT0 最小模板

```c
#include <REGX52.H>

// INT0 中断服务函数（下降沿触发）
void INT0_Routine() interrupt 0 {
    // 中断处理逻辑
    P2_0 = ~P2_0;  // 例如：翻转 LED
}

void main() {
    // 1. 设置触发方式
    IT0 = 1;    // 下降沿触发
    
    // 2. 使能中断
    EX0 = 1;    // 开 INT0 中断
    EA = 1;     // 开总中断
    
    // 3. 主循环
    while (1) {
        // 主程序逻辑
    }
}
```

### 5.2 双中断（INT0 + Timer0）模板

```c
#include <REGX52.H>

// INT0 中断 — 按键触发
void INT0_Routine() interrupt 0 {
    // 处理外部事件
}

// Timer0 中断 — 1ms 定时
void Timer0_Routine() interrupt 1 {
    static unsigned int count = 0;
    TH0 = (65536 - 1000) / 256;
    TL0 = (65536 - 1000) % 256;
    
    count++;
    if (count >= 1000) {
        count = 0;
        // 每秒执行
    }
}

void main() {
    // INT0 初始化
    IT0 = 1;  EX0 = 1;
    
    // Timer0 初始化（模式1，1ms）
    TMOD &= 0xF0;  TMOD |= 0x01;
    TH0 = (65536 - 1000) / 256;
    TL0 = (65536 - 1000) % 256;
    ET0 = 1;  TR0 = 1;
    
    EA = 1;  // 最后开总中断
    
    while (1) {
        // 主循环
    }
}
```

### 5.3 中断优先级示例

```c
// 场景：Timer0 作为高优先级（不能被打断），INT0 作为低优先级

void main() {
    // 配置中断源
    IT0 = 1;  EX0 = 1;   // INT0
    ET0 = 1;  TR0 = 1;   // Timer0
    
    // 设置优先级：Timer0 高优先级
    PT0 = 1;   // Timer0 = 高优先级
    PX0 = 0;   // INT0 = 低优先级
    
    EA = 1;
    
    while (1);
}
```

---

## 六、预习问题清单

> 动手实验前，带着这些问题去理解：

1. **外部中断**：如果按键接在 P3.2，按下时产生下降沿，INT0 中断会触发几次？是否需要消抖？
2. **触发方式**：IT0=0（低电平触发）和 IT0=1（下降沿触发）在按键场景下的行为差异是什么？哪种更适合按键？
3. **中断嵌套**：如果 Timer0 中断正在执行（耗时较长），INT0 触发后能否打断 Timer0？什么条件下可以？
4. **中断 vs 轮询**：矩阵键盘扫描用中断好还是轮询好？各有什么优缺点？
5. **PWM 线索**：如果要产生 PWM 波形（LED 亮度渐变），应该用哪个定时器？中断频率如何计算？

---

## 七、与已学知识的关联

| 已学知识 | 与中断系统的关联 |
|----------|------------------|
| Timer0 定时器 | 已经在用中断（`interrupt 1`），这次深入理解中断机制本身 |
| 独立按键 | 按键检测从"轮询消抖"升级为"中断触发"（INT0/INT1） |
| 矩阵键盘 | 理论上可以用中断替代逐行扫描（但需要额外硬件支持） |
| LED 输出 | 中断实验最简单的观察对象（中断→LED 翻转/亮灭） |

---

## 📊 预习小结

| 维度 | 掌握程度 |
|------|----------|
| 5 个中断源及入口地址 | ✅ 已记忆 |
| IE / TCON / IP 寄存器 | 🔨 理解但需实操巩固 |
| 外部中断触发方式选择 | ✅ 理解差异 |
| 中断优先级和嵌套规则 | 🔨 需实验验证 |
| 中断服务函数编写 | ✅ 可与定时器 ISR 类比 |

---

*整理于 2026-07-10，明天开始动手实验 🚀*
