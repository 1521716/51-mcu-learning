# DS18B20 温度传感器 — 理论预习笔记

> 预习时间：2026-07-18 | 前置知识：I2C 协议（已完成）、定时器中断（已完成）
>
> 下一步：DS18B20 温度读取 → LCD1602 实时温度显示 → 温度报警器

---

## 一、DS18B20 是什么

**DS18B20** 是 Dallas Semiconductor（现 Maxim Integrated）生产的数字温度传感器。

核心特性：
- **单总线（OneWire）协议** — 只需要 1 根数据线 + 地线（VCC 可选）
- 测温范围：-55°C ~ +125°C
- 分辨率：9~12 位可编程（默认 12 位，精度 0.0625°C）
- 每个芯片有全球唯一的 64 位 ROM 序列号 — 一根总线上可以挂多个 DS18B20
- 寄生供电模式：可以从数据线"偷电"，不需要外接 VCC（但转换时数据线需强上拉）

```
       ┌──────────┐
       │ DS18B20  │
       │          │
  GND──┤1       3├── VCC (可选)
       │    2    │
       └────┬────┘
            │
       DQ (数据线)
       │
       ├── 4.7kΩ 上拉电阻 ── VCC
       │
       └── MCU GPIO (如 P3.7)
```

---

## 二、OneWire 单总线协议

### 2.1 为什么叫"单总线"？

只有一根数据线（DQ），既传数据又传命令，双向通信。主设备（MCU）通过拉低/释放总线来控制时序，从设备（DS18B20）通过拉低总线来应答。

### 2.2 与 I2C/UART 的对比

| 特性 | OneWire | I2C | UART |
|------|---------|-----|------|
| 线数 | 1 根数据线 | 2 根 (SCL+SDA) | 2 根 (TX+RX) |
| 时钟 | 无独立时钟线 | 独立时钟线 SCL | 异步（无时钟线） |
| 时序精度 | 微秒级（苛刻！） | 微秒级（较宽松） | 毫秒级（最宽松） |
| 速度 | 慢（~16kbps） | 标准 100kbps | 常用 9600bps |
| 多设备 | 支持（通过 ROM ID 寻址） | 支持（通过器件地址） | 不支持（点对点） |
| 硬件校验 | CRC8 | 应答位 | 无/奇偶校验位 |

### 2.3 核心时序

DS18B20 的时序要求比 I2C 更苛刻，需要微秒级精确延时。

```
【初始化/复位脉冲】
MCU 拉低 DQ >= 480μs → 释放 DQ → DS18B20 拉低 DQ 60~240μs (应答/存在脉冲)

【写 0】
MCU 拉低 DQ >= 60μs (一般 60~120μs) → 释放 DQ → 恢复高电平 >= 1μs

【写 1】
MCU 拉低 DQ >= 1μs (一般 1~15μs) → 释放 DQ → 保持高电平 >= 60μs

【读 0/1】
MCU 拉低 DQ >= 1μs → 释放 DQ → 立即读 DQ 电平 → 保持 >= 60μs
                                       │
                                  此时 DQ=0 → 读 0
                                  此时 DQ=1 → 读 1
```

---

## 三、DS18B20 的命令体系

### 3.1 ROM 命令（器件寻址）

| 命令 | 代码 | 说明 |
|------|------|------|
| SEARCH ROM | 0xF0 | 搜索总线上所有器件的 ROM ID |
| READ ROM | 0x33 | 读单个器件的 64 位 ROM ID（只适合总线上只有一个器件） |
| MATCH ROM | 0x55 | 指定 64 位 ROM ID，选中特定器件 |
| SKIP ROM | 0xCC | 跳过 ROM 寻址，直接操作（单器件时常用） |
| ALARM SEARCH | 0xEC | 搜索温度超过报警阈值的器件 |

### 3.2 功能命令（操作传感器）

| 命令 | 代码 | 说明 |
|------|------|------|
| CONVERT T | 0x44 | 启动温度转换（转换时间：12-bit 模式下 ~750ms） |
| WRITE SCRATCHPAD | 0x4E | 写暂存器（设置分辨率、报警阈值） |
| READ SCRATCHPAD | 0xBE | 读暂存器（获取温度值） |
| COPY SCRATCHPAD | 0x48 | 将暂存器复制到 EEPROM（掉电保存设置） |
| RECALL E2 | 0xB8 | 从 EEPROM 恢复设置到暂存器 |
| READ POWER SUPPLY | 0xB4 | 读供电模式（寄生供电 or 外部供电） |

---

## 四、温度读取流程

### 4.1 标准流程（单器件）

```
1. 初始化 (复位 + 收应答脉冲)
2. SKIP ROM (0xCC)          — 跳过 ROM 寻址
3. CONVERT T (0x44)         — 启动温度转换
4. 等待 750ms (或轮询 DQ 直到变高) — 转换完成
5. 初始化 (复位 + 收应答脉冲)
6. SKIP ROM (0xCC)          — 跳过 ROM 寻址
7. READ SCRATCHPAD (0xBE)   — 读暂存器
8. 读 9 字节数据 (我们只需要前 2 字节温度值)
```

### 4.2 温度数据格式

暂存器前 2 字节为温度值（16-bit 有符号数，补码）：

```
Byte 0 (LSB):  b7  b6  b5  b4  b3  b2  b1  b0
               2^3 2^2 2^1 2^0 2^-12^-22^-32^-4

Byte 1 (MSB):  S   S   S   S   S   b10 b9  b8
               符号位 (1=负温度, 0=正温度)
```

**温度计算**：
```c
unsigned int temp_raw = (Byte1 << 8) | Byte0;
// 如果是正温度:
float temp = temp_raw * 0.0625;  // 默认 12-bit 分辨率
// 如果是负温度 (符号位 S=1):
// 取反加 1，再乘以 -0.0625
```

**常见温度值示例**：

| 温度 | 二进制 (MSB LSB) | 十六进制 |
|------|------------------|----------|
| +125°C | 0000 0111 1101 0000 | 0x07D0 |
| +25°C | 0000 0001 1001 0001 | 0x0191 |
| +0.5°C | 0000 0000 0000 1000 | 0x0008 |
| 0°C | 0000 0000 0000 0000 | 0x0000 |
| -0.5°C | 1111 1111 1111 1000 | 0xFFF8 |
| -25°C | 1111 1110 0110 1111 | 0xFE6F |

---

## 五、51 单片机代码模板

### 5.1 微秒延时（12T 模式 @ 11.0592MHz）

```c
// 约 1μs 延时 (11.0592MHz, 12T)
// 一个机器周期 = 12/11.0592 ≈ 1.085μs
// _nop_() = 1 机器周期
void Delay1us() {
    _nop_();
}

void DelayXus(unsigned char us) {
    while(us--) {
        _nop_();
        _nop_();
    }
}
```

### 5.2 单总线基本操作

```c
sbit DQ = P3^7;  // DS18B20 数据线

// 初始化：发送复位脉冲，等待应答
bit DS18B20_Reset() {
    bit ack;
    DQ = 0;              // 拉低总线
    DelayXus(240);       // 延时 480μs (两次拉低周期)
    DelayXus(240);
    DQ = 1;              // 释放总线
    DelayXus(60);        // 等待 15~60μs 后 DS18B20 会拉低总线
    ack = DQ;            // 读应答: 0=器件存在, 1=无器件
    DelayXus(240);       // 等待复位周期完成
    return ack;          // 返回 0 表示检测到 DS18B20
}

// 写一个位
void DS18B20_WriteBit(bit b) {
    DQ = 0;
    DelayXus(1);         // 拉低至少 1μs
    DQ = b;              // 写入数据位
    DelayXus(60);        // 保持至少 60μs (让从设备采样)
    DQ = 1;              // 释放总线
    DelayXus(1);
}

// 写一个字节 (低位在前!)
void DS18B20_WriteByte(unsigned char dat) {
    unsigned char i;
    for(i = 0; i < 8; i++) {
        DS18B20_WriteBit(dat & 0x01);
        dat >>= 1;
    }
}

// 读一个位
bit DS18B20_ReadBit() {
    bit b;
    DQ = 0;
    DelayXus(1);         // 拉低至少 1μs
    DQ = 1;              // 释放总线
    DelayXus(1);         // 稍等
    b = DQ;              // 读数据
    DelayXus(60);        // 等待读周期完成
    return b;
}

// 读一个字节 (低位在前!)
unsigned char DS18B20_ReadByte() {
    unsigned char i, dat = 0;
    for(i = 0; i < 8; i++) {
        if(DS18B20_ReadBit())
            dat |= (0x01 << i);
    }
    return dat;
}
```

### 5.3 读取温度

```c
float DS18B20_ReadTemp() {
    unsigned char TL, TH;
    int temp_raw;

    DS18B20_Reset();
    DS18B20_WriteByte(0xCC);   // SKIP ROM
    DS18B20_WriteByte(0x44);   // CONVERT T

    // 等待转换完成 (12-bit 模式需要 ~750ms)
    // 方式 1: 直接延时 800ms
    // Delay(800);
    // 方式 2: 轮询 DQ (DQ=0 表示正在转换, DQ=1 表示完成)
    while(!DQ);

    DS18B20_Reset();
    DS18B20_WriteByte(0xCC);   // SKIP ROM
    DS18B20_WriteByte(0xBE);   // READ SCRATCHPAD

    TL = DS18B20_ReadByte();   // 低字节
    TH = DS18B20_ReadByte();   // 高字节

    temp_raw = (TH << 8) | TL;
    return temp_raw * 0.0625;  // 12-bit 分辨率
}
```

---

## 六、常见踩坑点

| 坑 | 原因 | 解决 |
|----|------|------|
| 总是读到 0xFFFF 或 0x0000 | 时序不对、上拉电阻缺失 | 检查 4.7kΩ 上拉电阻、用逻辑分析仪确认波形 |
| 读到 85°C | DS18B20 上电默认值 (= 0x0550) | 确认 CONVERT T 命令执行成功，等待时间足够 |
| 温度值跳变/不准确 | 延时函数不够精确 | 禁止在读写过程中被中断打断（关总中断 EA=0） |
| 寄生供电模式下转换失败 | 转换时数据线没有强上拉 | 转换期间用 MOSFET 强上拉 DQ 到 VCC |
| 多个 DS18B20 读不到 | 使用 SKIP ROM 但有多个器件 | 先 SEARCH ROM 获取 ROM ID，再用 MATCH ROM 选择 |

---

## 七、预习检查清单

回答以下问题确认理解：

1. **DS18B20 用什么协议通信？几根线？**
   
2. **为什么要先在总线上发复位脉冲？**
   
3. **SKIP ROM (0xCC) 和 MATCH ROM (0x55) 的区别是什么？**
   
4. **一次温度读取需要哪几个步骤？**
   
5. **为什么 OneWire 时序中对延时精度要求这么高？**
   
6. **如果 DS18B20 总是返回 85°C，可能是什么原因？**

---

*参考资料: DS18B20 Datasheet, 江协科技 51 单片机教程 第 23 集*
