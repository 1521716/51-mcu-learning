# 51 单片机 PWM — 理论预习笔记

> 预习时间：2026-07-11 | 前置知识：定时器中断（已完成）、中断系统（已预习）
>
> 下一步：PWM 控制 LED 呼吸灯 → 蜂鸣器音调控制 → 舵机角度控制

---

## 一、什么是 PWM

**PWM（Pulse Width Modulation，脉冲宽度调制）** 是一种通过数字信号模拟模拟输出的技术。

核心思想：在一个固定周期内，控制高电平的持续时间（占空比），从而控制平均输出电压。

```
周期 T（固定，如 1ms）
│←     周期     →│←     周期     →│
▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁
▓▓▓▓▓▓▓░░░░░▓▓▓▓▓▓▓▓▓▓░░░░▓▓▓▓▓▓░░░░░░░
│← 高 →│← 低 →││←  高  →│← 低 →││←高→│←低→│
   70% 占空比      80% 占空比     30% 占空比
```

**关键公式**：

```
占空比 = (高电平时间 / 周期) × 100%
平均电压 = 占空比 × VCC（5V 系统中 VCC = 5V）
```

> 例如：占空比 50% → 平均电压 = 2.5V → LED 半亮；占空比 100% → 5V → LED 全亮

---

## 二、51 单片机实现 PWM 的方法

### 2.1 为什么 51 没有硬件 PWM？

STC89C52 的定时器本身**不直接支持 PWM 输出**（相比 STM32 的硬件 PWM 通道），需要用**软件配合定时器中断**来模拟。

> 注：增强型 51（STC12/STC15/STC8 系列）内置 PCA 模块，支持硬件 PWM。

### 2.2 软件 PWM 原理

利用 Timer0/Timer1 中断，在 ISR 中以固定节奏（如每 100μs）比较当前计数值和占空比阈值：

```
PWM 周期 = 100 个 Tick（每个 Tick = 100μs，即 10ms 周期 = 100Hz）
占空比 = duty（0~100 之间）

ISR 每秒中断 10000 次（100μs × 100 = 10ms 周期）

伪代码：
  count = (count + 1) % 100
  if count < duty:  LED = ON
  else:             LED = OFF
```

### 2.3 Timer0 配置方案

| 参数 | 取值 | 说明 |
|------|------|------|
| 工作模式 | 模式 1（16 位定时器）| 最大计数 65536 |
| 中断频率 | 100μs/次 | 10MHz 以下晶振需手动重装 |
| 重装值 | 65536 - 100（12MHz）| 12T 模式：1 机器周期 = 1μs |
| PWM 周期 | 100 个 Tick = 10ms | 频率 = 100Hz |
| 占空比精度 | 1%（0~100 可调）| 100 级灰度 |

---

## 三、代码模板

### 3.1 LED 呼吸灯（最小实现）

```c
#include <REGX52.H>

sbit LED = P2^0;

unsigned char pwm_count = 0;   // 当前 Tick（0~99）
unsigned char pwm_duty = 50;   // 占空比（0~100）
bit pwm_dir = 0;               // 0=渐亮(duty↑), 1=渐暗(duty↓)

// Timer0 中断 — 每 100μs 触发一次
void Timer0_Routine() interrupt 1 {
    // 手动重装（12MHz, 12T, 100μs）
    TH0 = (65536 - 100) / 256;
    TL0 = (65536 - 100) % 256;

    pwm_count++;
    if (pwm_count >= 100) {
        pwm_count = 0;  // 新的 PWM 周期

        // 呼吸效果：渐亮→渐暗→渐亮...
        if (pwm_dir == 0) {
            pwm_duty++;
            if (pwm_duty >= 100) pwm_dir = 1;
        } else {
            pwm_duty--;
            if (pwm_duty == 0) pwm_dir = 0;
        }
    }

    // PWM 输出
    if (pwm_count < pwm_duty)
        LED = 0;  // 低电平点亮（开发板 LED 共阳极）
    else
        LED = 1;  // 熄灭
}

void main() {
    // Timer0 初始化：模式 1，100μs 中断
    TMOD &= 0xF0;
    TMOD |= 0x01;
    TH0 = (65536 - 100) / 256;
    TL0 = (65536 - 100) % 256;
    ET0 = 1;
    TR0 = 1;
    EA = 1;

    while (1) {
        // 主循环空闲，PWM 全由中断驱动
    }
}
```

### 3.2 按键调节占空比（交互式）

```c
#include <REGX52.H>

sbit LED = P2^0;
sbit KEY_UP = P3^0;    // 增加占空比
sbit KEY_DOWN = P3^1;  // 减少占空比

unsigned char pwm_count = 0;
unsigned char pwm_duty = 0;   // 初始 0%（LED 灭）

void Timer0_Routine() interrupt 1 {
    TH0 = (65536 - 100) / 256;
    TL0 = (65536 - 100) % 256;

    pwm_count++;
    if (pwm_count >= 100) pwm_count = 0;

    // 按键扫描（放在中断里保证实时响应）
    if (KEY_UP == 0) {
        if (pwm_duty < 100) pwm_duty += 5;  // 每次 +5%
    }
    if (KEY_DOWN == 0) {
        if (pwm_duty > 0) pwm_duty -= 5;
    }

    // PWM 输出
    LED = (pwm_count < pwm_duty) ? 0 : 1;
}

void main() {
    TMOD &= 0xF0;  TMOD |= 0x01;
    TH0 = (65536 - 100) / 256;
    TL0 = (65536 - 100) % 256;
    ET0 = 1;  TR0 = 1;  EA = 1;
    while (1);
}
```

---

## 四、PWM 应用场景

| 场景 | 周期建议 | 占空比范围 | 关键点 |
|------|----------|-----------|--------|
| LED 呼吸灯 | 10ms（100Hz）| 0~100% | 100 级亮度，人眼无闪烁 |
| 蜂鸣器音调 | 1ms（1kHz）| 50% | 方波频率决定音调高低 |
| 蜂鸣器音量 | 固定频率 | 0~100% | 占空比控制响度 |
| 舵机控制 | 20ms（50Hz）| 5%~10%（0.5~2.5ms）| 0.5ms=0°, 1.5ms=90°, 2.5ms=180° |
| DC 电机调速 | 10ms | 0~100% | 配合 L298N/H 桥驱动 |

---

## 五、与已学知识关联

| 已学知识 | 与 PWM 的关联 |
|----------|--------------|
| Timer0 定时器 | PWM 的时间基准（Tick 发生器），完全基于 Timer0 中断 |
| 中断系统 | PWM ISR 是 Timer0 中断的扩展应用 |
| 独立按键 | 用于实时调节占空比（如灯光旋钮） |
| LED 输出 | PWM 最简单的验证对象（亮度渐变） |
| LCD1602 | 可显示当前占空比百分比（调试用） |

---

## 六、预习问题清单

1. **PWM 频率选择**：为什么 LED 呼吸灯选 100Hz 而不是 50Hz 或 1kHz？频率太低/太高会怎样？
2. **精度与开销**：100 级精度需要 100μs 中断一次，CPU 负载是多少？如果换成 256 级精度（8 位），CPU 还扛得住吗？
3. **多路 PWM**：如果需要 3 个 LED 同时独立呼吸（不同占空比），代码需要怎么改？
4. **硬件 PWM**：增强型 51（STC12/STC15）有 PCA 硬件 PWM，和软件 PWM 比有什么优势？
5. **蜂鸣器**：无源蜂鸣器需要方波才能发声，PWM 频率多少才能发出 440Hz 的 A4 音符？

---

## 📊 预习小结

| 维度 | 掌握程度 |
|------|----------|
| PWM 占空比/周期概念 | ✅ 已理解 |
| 51 软件 PWM 实现原理 | 🔨 理解但需实操 |
| Timer0 作为 PWM 时基 | ✅ 可复用定时器知识 |
| 呼吸灯代码逻辑 | 🔨 需实验验证渐变效果 |
| 舵机/蜂鸣器 PWM 参数 | 📝 已记录，实验时查阅 |

---

*整理于 2026-07-11，搬完校区回来动手 🔧*
