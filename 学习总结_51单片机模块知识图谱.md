# 51单片机模块知识图谱 — 阶段性学习总结

> 整理时间：2026-07-04 | 已学习 6 周（06.22 – 07.04）

---

## 知识体系总览

```
                    ┌─────────────────────────────────┐
                    │        51单片机 (STC89C52RC)      │
                    └─────────────────────────────────┘
                                    │
        ┌───────────┬───────────────┼───────────────┬───────────┐
        │           │               │               │           │
      GPIO      中断/定时器      显示系统        输入系统      通信
        │           │               │               │           │
    ┌───┴───┐   ┌───┴───┐     ┌───┴───┐     ┌───┴───┐    (待学)
    │       │   │       │     │       │     │       │
   LED   独立   定时器  外部  数码管  LCD   独立   矩阵
  输出   按键   计数器  中断   (静态  1602   按键   键盘
  P1口  P3口   (已学) (待学)   +动态) (已学) (已学) (已学)
 (已学) (已学)                  (已学)
```

---

## 模块一：GPIO 输出 — LED 控制

**核心寄存器**：P1（0x90）

**关键操作**：
```c
sbit LED = P1^0;           // 位寻址
P1 = ~(1 << n);            // 移位操作点亮第n位
P1 = 0xFF;                 // 全部熄灭（共阳极）
```

**已掌握程度**：⭐⭐⭐⭐⭐ 可盲写
- LED 闪烁（软件延时 500ms）
- LED 流水灯（移位 + 循环）
- 位运算组合点亮任意 LED

---

## 模块二：GPIO 输入 — 独立按键

**核心寄存器**：P3（0xB0）

**关键模式**：
```c
unsigned char Key_Scan() {
    static unsigned char last_state = 1;
    unsigned char curr = KEY;
    if (last_state == 1 && curr == 0) {  // 下降沿检测
        Delay(20);                        // 消抖
        if (KEY == 0) {                   // 确认
            last_state = 0;
            return 1;                     // 有效按下
        }
    }
    if (curr == 1) last_state = 1;        // 释放后复位
    return 0;
}
```

**已掌握程度**：⭐⭐⭐⭐⭐ 可盲写
- 按键消抖原理（20ms延时 + 二次确认）
- 下降沿检测（last_state==1 && curr==0）
- static 变量保持上一次状态

---

## 模块三：数码管显示

**原理**：共阳极数码管 → 位选（P2.0-P2.7） + 段选（P0），通过快速切换实现"人眼同时看见"

**段码表**（共阳极）：
```c
unsigned char Nixie[10] = {
    0xC0, 0xF9, 0xA4, 0xB0, 0x99,  // 0-4
    0x92, 0x82, 0xF8, 0x80, 0x90   // 5-9
};
```

**动态扫描关键**：先关位选→设段选→开位选→延时1ms→下一位置

**消影**：每次切换位选前关闭所有位选（P2=0xFF），消除上一位置的残影

**已掌握程度**：⭐⭐⭐⭐
- 静态显示（单个数码管显示0-9）
- 动态扫描（3位数码管同时显示不同数字）
- 密码锁数码管反馈（PASS/FAIL）

---

## 模块四：密码锁状态机

**设计模式**：
```
状态: SLOW_BLINK → KEY_COUNT → TIMEOUT_JUDGE → SUCCESS / FAIL
      (慢闪等待)   (按键计数)   (5秒超时判断)   (常亮/快闪)
```

**状态转移条件**：
- SLOW_BLINK → KEY_COUNT：检测到按键按下
- KEY_COUNT → KEY_COUNT：继续计数（count < 密码长度）
- KEY_COUNT → TIMEOUT_JUDGE：计数达标，启动5秒定时器
- TIMEOUT_JUDGE → SUCCESS：5秒内再次按键（确认）
- TIMEOUT_JUDGE → FAIL：5秒超时无操作

**已掌握程度**：⭐⭐⭐⭐
- 两种密码锁实现（LED输出 + 数码管输出）
- 状态机思维：每个状态独立处理，条件触发转移

---

## 模块五：矩阵键盘（4×4）

**原理**：8个GPIO（4行+4列），逐行扫描 → 检测哪一列的按键被按下

**扫描算法**：
```c
unsigned char MatrixKey_Scan() {
    for (row = 0; row < 4; row++) {
        P1 = ~(0x10 << row);        // 逐行置低
        for (col = 0; col < 4; col++) {
            if ((P1 & (0x01 << col)) == 0) {  // 检测列
                Delay(20);                     // 消抖
                while ((P1 & (0x01 << col)) == 0); // 等待释放
                return row * 4 + col + 1;      // 返回键值 1-16
            }
        }
    }
    return 0;
}
```

**已掌握程度**：⭐⭐⭐⭐
- 矩阵键盘扫描原理（逐行置低，逐列检测）
- LCD1602 实时显示键值 (1-16)
- 矩阵键盘密码锁（密码=1716，S11确认/S12清除）

---

## 模块六：LCD1602 字符液晶

**关键指令**：
| 指令 | RS | RW | DB7-DB0 |
|------|-----|-----|---------|
| 清屏 | 0 | 0 | 0x01 |
| 光标归位 | 0 | 0 | 0x02 |
| 设置显示模式 | 0 | 0 | 0x38（8位/2行/5×7） |
| 显示开关 | 0 | 0 | 0x0C（开显示/关光标） |

**驱动库函数**：
```c
void LCD_WriteCommand(unsigned char cmd);   // RS=0, RW=0
void LCD_WriteData(unsigned char dat);       // RS=1, RW=0
void LCD_Init();                             // 初始化序列
void LCD_ShowChar(row, col, ch);            // 指定位置显示字符
void LCD_ShowString(row, col, str);         // 显示字符串
void LCD_ShowNum(row, col, num, len);       // 显示无符号数
void LCD_ShowSignedNum(row, col, num, len); // 显示有符号数
```

**已掌握程度**：⭐⭐⭐（能调用，驱动原理待深入）

---

## 模块七：定时器/计数器

**Timer0 配置**：
```c
void Timer0_Init() {
    TMOD &= 0xF0;       // 清除低4位
    TMOD |= 0x01;       // 模式1（16位定时器）
    TH0 = (65536 - 1000) / 256;   // 1ms定时
    TL0 = (65536 - 1000) % 256;
    ET0 = 1;            // 使能Timer0中断
    EA = 1;             // 开总中断
    TR0 = 1;            // 启动定时器
}
```

**中断服务函数**：
```c
void Timer0_ISR() interrupt 1 {
    TH0 = (65536 - 1000) / 256;   // 手动重装
    TL0 = (65536 - 1000) % 256;
    // 每1ms执行一次的任务
}
```

**已掌握程度**：⭐⭐⭐（能用，不同模式待学习）

---

## 📊 总体评估

| 维度 | 评级 | 说明 |
|------|------|------|
| GPIO 控制 | ⭐⭐⭐⭐⭐ | 可盲写 LED/按键驱动 |
| 显示系统 | ⭐⭐⭐⭐ | 数码管+LCD1602 均已实践 |
| 状态机思维 | ⭐⭐⭐⭐ | 密码锁双版本实现 |
| 定时器 | ⭐⭐⭐ | 基础中断使用 OK |
| 通信协议 | ⬜ | I2C/SPI/UART 待学习 |

**已累计代码项目**：21 个 Keil5 工程

**下一步**：中断系统 + PWM → 蜂鸣器驱动 → 串口通信 → 综合密码锁

---

*考试结束后继续推进*
